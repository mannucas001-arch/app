{
  "name": "vet-erp",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n API,WEB -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client -- --host 127.0.0.1\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
